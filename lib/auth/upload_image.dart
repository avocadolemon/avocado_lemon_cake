import 'package:flutter/material.dart';

class UplaodProfileImage extends StatelessWidget {
  const UplaodProfileImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(children: const []),
    );
  }
}
